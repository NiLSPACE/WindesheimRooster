<Page
    x:Class="WindesheimRooster.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindesheimRooster"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48*"/>
            <RowDefinition Height="541*"/>
            <RowDefinition Height="51*"/>
        </Grid.RowDefinitions>
        <ListView x:Name="lvClassList" Margin="0,10,0,8" SelectionChanged="lvClassList_SelectionChanged" Grid.Row="1" Grid.RowSpan="1"/>
        <TextBox x:Name="tbFilter" Margin="10,10,10,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" PlaceholderText="Filter" TextChanged="tbFilter_TextChanged" IsEnabled="False" Height="32"/>
        <CommandBar Grid.Row="2" Margin="0,0,0,0" VerticalAlignment="Stretch">
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton Icon="Clock" Label="History" Click="History_Click"/>
        </CommandBar>
    </Grid>
</Page>
